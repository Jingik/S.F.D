# ?????? ??
spring.application.name=backend
server.port=8080

# ?????? ?? ?? (MySQL)
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.url=jdbc:mysql://J11B103.p.ssafy.io/sfd?useSSL=false&useUnicode=true&serverTimezone=Asia/Seoul
spring.datasource.username=root
spring.datasource.password=s11p22b103!

# JPA ? Hibernate ??
spring.jpa.show-sql=true
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.format_sql=true

# DDL (??? ???) ?? ? ??? ?? (??? ?? ?? ??)
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE

# ?? ??? ?? (?? ?? ??? ??)
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB
spring.servlet.multipart.enabled=true

# JWT ?? ??
jwt.access-token-validity-in-seconds=3600
jwt.refresh-token-validity-in-seconds=86400

# application.properties
logging.level.org.springframework.security=DEBUG